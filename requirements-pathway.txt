# Windows-compatible requirements (without ML dependencies)
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
streamlit==1.38.0
requests==2.32.3
numpy==1.26.4
pandas==2.2.2
python-dotenv==1.0.1
pytest>=7.0.0
python-multipart
aiofiles
geopy>=2.2.0
folium>=0.12.0

# Optional: Install these only if you want ML features
# sentence-transformers>=2.2.2  # Requires PyTorch
# scikit-learn>=1.5.2          # May have Windows issues
# pathway                      # Linux/WSL only
# Pathway framework (Linux/WSL only)
pathway>=0.7.0

# Optional ML dependencies
sentence-transformers>=2.2.2
scikit-learn>=1.5.2

# Additional dependencies for real-time features
websockets>=11.0
